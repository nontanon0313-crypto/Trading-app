# Gemini APIキー(aistudio.google.comで無料取得)
GEMINI_API_KEY=your_api_key_here

# データベース(デフォルトはローカルSQLite。変更不要)
DATABASE_URL=sqlite:///./data/fx_trade.db

# PWAフロントエンドのURLを指定(セキュリティのため本番では*にしない)
ALLOWED_ORIGINS=*
